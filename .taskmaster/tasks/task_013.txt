# Task ID: 13
# Title: Implement Admin UI Type Definitions
# Status: pending
# Dependencies: 2, 11
# Priority: medium
# Description: Create type definitions for Admin UI components, views, and customization options.
# Details:
Define Admin UI types in `types/admin/index.d.ts`:

```typescript
import * as express from 'express';
import { KeystoneList } from '../lib/list';
import { KeystoneField } from '../fields';

// Admin UI options
export interface AdminUIOptions {
  path?: string;
  signin?: SigninOptions;
  signout?: SignoutOptions;
  nav?: NavConfig;
  static?: string | string[];
  favicon?: string;
  sass?: string | SassOptions;
  less?: string | LessOptions;
  styles?: string | string[];
  scripts?: string | string[];
  compress?: boolean;
  views?: string | string[];
  templates?: string;
}

// Signin options
export interface SigninOptions {
  url?: string;
  redirect?: string;
  view?: string;
  logo?: string;
  brand?: string;
}

// Signout options
export interface SignoutOptions {
  url?: string;
  redirect?: string;
}

// Navigation configuration
export type NavConfig = NavSection[] | Record<string, string | string[]>;

export interface NavSection {
  label: string;
  key?: string;
  lists?: string[];
  items?: NavItem[];
}

export interface NavItem {
  label: string;
  key: string;
  path?: string;
  lists?: string[];
}

// Sass options
export interface SassOptions {
  src?: string;
  dest?: string;
  outputStyle?: 'nested' | 'expanded' | 'compact' | 'compressed';
  sourceMap?: boolean;
  sourceComments?: boolean;
  includePaths?: string[];
}

// Less options
export interface LessOptions {
  src?: string;
  dest?: string;
  compress?: boolean;
  sourceMap?: boolean;
  paths?: string[];
}

// View configuration
export interface ViewConfig {
  name: string;
  path: string;
}

// Admin UI view
export interface AdminUIView {
  render(req: express.Request, res: express.Response, next: express.NextFunction): void;
  renderResponse(req: express.Request, res: express.Response, locals: Record<string, any>): void;
}

// Field UI configuration
export interface FieldUIOptions {
  label?: string;
  path?: string;
  initial?: boolean;
  required?: boolean;
  noedit?: boolean;
  nocol?: boolean;
  nosort?: boolean;
  nofilter?: boolean;
  note?: string | ((item: any) => string);
  collapse?: boolean;
  dependsOn?: Record<string, any>;
  watch?: boolean | Record<string, any>;
}

// List UI configuration
export interface ListUIOptions {
  label?: string;
  singular?: string;
  plural?: string;
  path?: string;
  defaultSort?: string;
  defaultColumns?: string;
  hidden?: boolean;
  track?: boolean | { createdAt?: boolean; updatedAt?: boolean };
  perPage?: number;
  searchFields?: string | string[];
  sortable?: boolean;
  sortContext?: string;
  defaultSort?: string;
  defaultFilters?: Record<string, any>;
  autocreate?: boolean;
  nodelete?: boolean;
  noedit?: boolean;
  nocreate?: boolean;
  map?: Record<string, string>;
  csv?: boolean | string[];
  exportCSV?: boolean | string[];
  selectColumns?: string[];
  selectFilters?: string[];
  expandColumns?: string[];
}

// Admin UI factory functions
export function createAdminUIApp(options: AdminUIOptions): express.Application;
export function createAdminUIRouter(options: AdminUIOptions): express.Router;
export function createAdminUIMiddleware(options: AdminUIOptions): express.RequestHandler;
```

Ensure all interfaces have proper JSDoc comments explaining their purpose and usage. Include proper types for Admin UI customization options.

# Test Strategy:
Create test cases that import and use these Admin UI types in configuration scenarios. Verify that the TypeScript compiler correctly enforces type constraints. Test with actual Keystone Admin UI usage patterns to ensure the interfaces match real-world usage.
