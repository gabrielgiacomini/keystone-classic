# Admin Modernization Parity Ledger

Last updated: 2026-05-24

This ledger makes the admin behavior that must survive modernization explicit.
Status values are conservative: a row is complete only when there is direct
implementation and verification evidence.

## Workflow Parity

| Workflow | Legacy owner | Admin-next owner | Status | Required evidence |
| --- | --- | --- | --- | --- |
| signin | `admin/client-legacy/Signin` | `admin/client-next/src/routes/signin.tsx` | Complete | `e2e-ui/tests/parity/auth.spec.ts` covers visible forms, valid credential redirect, invalid credential errors, stale-CSRF 403 handling with refresh guidance in both clients, shared session signin POST status in both clients, and anonymous list/item protected-route redirects to each client's signin page. |
| signout | `admin/server/routes-legacy/signout.mts` | `admin/client-next/src/routes/signout.tsx` | Complete | `e2e-ui/tests/parity/auth.spec.ts` covers signout route redirect to each client's signin page plus anonymous protected-route re-redirect coverage for list and item deep links. |
| home/dashboard | `admin/client-legacy/App/screens/Home` | `admin/client-next/src/routes/home.tsx` | Complete | `e2e-ui/tests/parity/home.spec.ts` covers dashboard rendering, matching list keys, matching sectioned navigation groups/list paths, external nav entries without create affordances, orphaned-list rendering in the `Other` section, Post count parity against Mongo, and dashboard Post manage/create links opening the matching list view and create modal in both clients. |
| list navigation | legacy list screen/router | `admin/client-next/src/routes/$list.tsx` | Complete | `e2e-ui/tests/parity/home.spec.ts` and `e2e-ui/tests/parity/list.spec.ts` cover home-to-list availability, direct list key/path deep links, and invalid list/list-item deep links showing the legacy-compatible "List not found!" fallback without rendering table/item forms or calling invalid admin-next list APIs. `test/unit/admin/server/app/admin-surfaces.test.mts` covers next-only cutover serving admin-next from both the historical `/keystone` path and `/keystone-next`, mount-aware admin-next bootstrap paths for each surface, and custom historical admin path cutover for `/manage`, `/manage/signin`, `/manage/signout`, item deep links, `/manage-next` alias paths, canonical custom API paths, and the legacy API alias. |
| list search | legacy list reducers/sagas/API | admin-next list route + API helpers | Complete | `e2e-ui/tests/parity/list.spec.ts` covers matching search result counts for an existing title, slug-backed multi-field search returning the same Mongo row in both clients, no-match searches that show the searched term in both clients, Escape clearing the search input/URL state while restoring rows in both clients, and non-empty search input waiting through the shared debounce window before loading results in both clients. |
| list sort | legacy list reducers/sagas/API | admin-next list route + API helpers | Complete | `e2e-ui/tests/parity/list.spec.ts` covers title ascending sort first-row parity, descending `sort=-title` parity against Mongo ordering, multi-field `priority,title` numeric select sort parity against Mongo ordering, multi-field `-reviewedAt,title` datetime sort parity against Mongo ordering, and multi-author `author,title` relationship sort parity against Mongo ordering in both clients. |
| list pagination | legacy list reducers/sagas/API | admin-next list route + shared list route state | Complete | Shared page-window/count helper unit tests exist. `e2e-ui/tests/parity/list.spec.ts` covers `page=2` deep links and page-button navigation across legacy and admin-next with matching summaries and row counts, a custom `perPage: 3` list proving both clients request matching `limit`/`skip` values and render matching summaries, out-of-range deep links preserving legacy skip behavior while rendering the blank-state list screen without table/pagination chrome, delete-induced page-boundary blank-state behavior after a final page row is removed, and empty-list blank-state create affordances opening the create modal in both clients. |
| list column configuration | legacy columns UI | admin-next list route + shared list route state | Complete | Shared default/active column helper unit tests exist. `e2e-ui/tests/parity/list.spec.ts` covers legacy `columns` query parity, admin-next `cols`/`columns` aliases, explicit URL column-order preservation, explicit relationship-column rendering for `author`, explicit special-field column rendering for code/money/color fields, interactive Columns dropdown updates to visible headers and URL state, admin-next Reset returning to the legacy default headers, and column-picker search narrowing to matching fields in both clients. |
| list filters | legacy filter components | admin-next field filters | Complete | `e2e-ui/tests/parity/list.spec.ts` covers URL-backed `state=published` select filter parity with matching legacy/admin-next row IDs, URL-backed `publishedAt` date filter parity with Mongo date verification, admin-next active filter chip display, interactive Filter dropdown application for the `state` select filter in both clients, interactive structured text, textarea, key, URL, email, name, code, color, HTML, Markdown, textarray, numberarray, and datearray mode filtering in both clients with matching row IDs and chip text, interactive `publishedAt` date after-filter application in both clients with matching row IDs and chip text, interactive greater-than and between number filter application in both clients with matching row IDs and chip text, interactive money less-than filtering in both clients with matching row IDs and chip text, interactive structured location postcode filtering in both clients with matching row IDs and chip text, interactive password existence filtering in both clients with matching row IDs and chip text, interactive boolean filtering in both clients with matching row IDs and chip text, interactive geopoint proximity filtering in both clients with matching row IDs and chip text, structured inverted multi-select URL parity, and structured inverted text-filter mode parity with matching row IDs plus chip text. Shared list route state unit coverage serializes structured text filter modes, structured textarray, numberarray, and datearray presence/mode filters, structured date/datearray/datetime filter modes including after and inverted between, password existence filters, boolean string/structured values, money chip labels, structured location filters and chip labels, and geopoint chip labels; the field-complete e2e suite covers admin-next filter controls and API filters across the field-complete fixture lists. |
| CSV download | legacy download API/link | admin-next list route/API | Complete | `e2e-ui/tests/parity/list-download.spec.ts` covers admin-next CSV/JSON export URLs, response headers, search preservation, legacy-vs-next search-filtered CSV URL generation, matching filtered CSV output, sorted `sort=-title` CSV URL/body ordering parity, field-filtered selected-column CSV export parity for `state=published`, selected-column preservation, custom Download-panel column selection with matching CSV output, spreadsheet-formula escaping for CSV values, and JSON format switching. `test/unit/admin/server/api/list-download.test.mts`, `test/unit/admin/server/api/download.test.mts`, and `test/unit/admin/server/api/download-limit.test.mts` cover export serialization, custom CSV row handling, JSON output, populate guards, and download limits. |
| create modal | legacy create modal | `CreateItemModal` and create route | Complete | `e2e-ui/tests/parity/create.spec.ts` covers creating Post items in legacy and admin-next, cross-UI count visibility, canceling the create modal in both clients without a create POST or count change, and required initial field validation in both clients with modal persistence and unchanged Mongo counts. |
| item edit | legacy item screen | `admin/client-next/src/routes/$list.$id.tsx` | Complete | `e2e-ui/tests/parity/item.spec.ts` covers loading the same item, title edit/save in each client, required title validation in both clients with item-open/stored-value preservation, cross-UI persisted values, User name/email edits, cross-UI text/key/code/color/money/textarray/numberarray/datearray/geopoint/location-address/location-geo/location-improve-controls/markdown/html/textarea/url/number/select/boolean multi-field edits, password mismatch validation with stored-hash preservation, cross-UI date/datetime edits with Mongo verification, and DST-boundary datetime round trips that preserve local wall-clock values across both clients. The field-complete e2e suite covers additional admin-next create/edit round trips for account identity, editorial scalar, relationship, location, geopoint, event date/money/number/date/datetime/boolean, product key/color/array/commerce, and media fields. |
| item delete | legacy item delete flow | admin-next confirm/delete flow | Complete | `e2e-ui/tests/parity/item.spec.ts` covers single-item delete from legacy and admin-next, Mongo removal, redirect to list, cross-UI absence, canceling the delete dialog in both clients without a delete POST while preserving the item, and admin-next failed delete responses keeping the item open with a visible error while preserving the Mongo item. |
| bulk delete | legacy list bulk action | admin-next list route | Complete | `e2e-ui/tests/parity/bulk.spec.ts` covers selecting two rows, confirming bulk delete in both clients, Mongo count reduction, cross-UI count consistency after legacy delete, visible-page All/None selection controls updating selected counts in both clients, and cross-page All selection deleting every row from a paginated `perPage: 3` list in both clients. |
| relationship select/search | legacy relationship field | admin-next relationship field | Complete | `e2e-ui/tests/parity/relationships.spec.ts` covers matching seeded single/many relationship labels, candidate-search exclusion behavior, optional empty single/many relationship render/save/reload parity, legacy single-relationship save reloaded in admin-next, admin-next many-relationship removal reloaded in legacy, relationship create-inline creation/selection/persistence in both clients, and required single/many relationship validation in both clients with stored-value preservation; `e2e-ui/tests/parity/fields.spec.ts` covers admin-next single/many save/remove/reload persistence. |
| inverse relationship panels | legacy item relationship panels | `InverseRelationshipPanel` | Complete | `e2e-ui/tests/parity/relationships.spec.ts` covers authored/editing inverse panels for admin and Alice users, including empty-state parity, admin-next paginated totals, navigating from inverse-panel related Post links to the matching item routes in both clients, admin-next Add Item carrying the parent relationship into the create payload, admin-next panel search preserving parent relationship filters with matching and empty-result states, and sortable inverse-panel reordering that preserves the parent relationship filter and stored sort order. |
| upload flows | legacy upload fields/API | admin-next upload fields/API | Complete | `e2e-ui/tests/fields/media-upload.spec.ts` covers legacy Cloudinary preview/upload/lightbox behavior, legacy file upload URL resolution, and admin-next upload/replace/remove for `file`, `cloudinaryimage`, `cloudinaryimages`, and direct `cloudinary` single/multiple fields using hermetic field-complete storage and Cloudinary mocks. `test/unit/admin/server/api/cloudinary.test.mts`, `test/unit/admin/server/api/file.test.mts`, and `test/unit/admin/server/api/s3.test.mts` cover upload API CSRF rejection, missing file handling, provider response normalization, safe file metadata, unsafe extension rejection, and S3 configuration/upload behavior. |
| field explorer | legacy field e2e harness | field-complete admin-next suite | Complete | `npm run test:e2e-ui:fields` passes the field-complete admin-next suite, including `e2e-ui/tests/fields/media-upload.spec.ts`; `e2e-ui/tests/react17-events.spec.ts` covers field explorer rendering through the legacy event-boundary harness, and package verification keeps the field explorer browser source JSX-free and on the esbuild/shared Less path. |
| custom field compatibility | legacy `FieldTypes` runtime bundle | `admin/shared/fields/legacyAdapters.ts` + modern registry | Complete | `test/unit/admin/shared/fields/legacyAdapters.test.mts` covers adapting legacy `Field`, `Filter`, and `Column` components, legacy `{ path, value }` field change unwrapping, upload-style object payload preservation, relationship-like column metadata, and registering legacy component sets under custom field type names with `registerLegacyFieldComponents()`. `test/unit/admin/shared/fields/registry.test.mts` covers custom registry keys without weakening built-in completeness checks. `test/unit/admin/shared/fields/customFields.test.mts` covers registering modern and legacy custom field component sets from the `window.Keystone` runtime bootstrap before admin-next renders. `test/unit/admin/server/app/admin-surfaces.test.mts` covers `admin next custom field scripts` injection before the app bundle. `test/unit/scripts/build-admin-next-custom-fields.test.mts` covers producing a same-origin custom field module script from an operator entry and executing the emitted script through the same runtime `Keystone.legacyFieldComponents` registration path admin-next uses. `docs/admin-next-custom-field-migration.md`, `docs/admin-modernization-upgrade-guide.md`, `package:verify`, and public type smoke coverage document and verify the supported adapter/registry/custom-field module-script surfaces plus unsupported legacy package surfaces. |

## Built-In Field Parity

| Field type | Legacy UI owner | Admin-next owner | Field | Filter | Column | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| boolean | `fields/types/boolean/*.mjs` | `admin/client-next/src/fields/boolean` | Present | Present | Present | Complete | `e2e-ui/tests/parity/item.spec.ts` covers legacy-to-next and next-to-legacy boolean edit persistence with Mongo verification; `e2e-ui/tests/parity/list.spec.ts` covers interactive boolean list filtering in both clients |
| cloudinary | `fields/types/cloudinary/*.mjs` | `admin/client-next/src/fields/cloudinary` | Present | Present | Present | Complete | `e2e-ui/tests/fields/media-upload.spec.ts` covers admin-next direct `Types.Cloudinary` single and multiple upload, image render, reload, and remove persistence with hermetic Cloudinary mock responses |
| cloudinaryimage | `fields/types/cloudinaryimage/*.mjs` | `admin/client-next/src/fields/cloudinaryimage` | Present | Present | Present | Complete | `e2e-ui/tests/fields/media-upload.spec.ts` covers legacy preview/upload/lightbox behavior and admin-next upload/render/remove persistence for deprecated `Types.CloudinaryImage` compatibility |
| cloudinaryimages | `fields/types/cloudinaryimages/*.mjs` | `admin/client-next/src/fields/cloudinaryimages` | Present | Present | Present | Complete | `e2e-ui/tests/fields/media-upload.spec.ts` covers legacy gallery preview/lightbox behavior and admin-next upload/render/remove persistence for deprecated `Types.CloudinaryImages` compatibility |
| code | `fields/types/code/*.mjs` | `admin/client-next/src/fields/code` | Present | Present | Present | Complete | `e2e-ui/tests/parity/item.spec.ts` covers legacy-to-next and next-to-legacy CodeMirror edit persistence with Mongo verification; admin-next CodeMirror now initializes from the latest item value after lazy editor imports resolve; `e2e-ui/tests/parity/list.spec.ts` covers interactive code mode filtering in both clients with matching row IDs, Mongo verification, and admin-next chip text |
| color | `fields/types/color/*.mjs` | `admin/client-next/src/fields/color` | Present | Present | Present | Complete | `e2e-ui/tests/parity/item.spec.ts` covers legacy-to-next and next-to-legacy color edit persistence on the parity Post fixture with Mongo verification; `e2e-ui/tests/parity/list.spec.ts` covers interactive color mode filtering in both clients with matching row IDs, Mongo verification, and admin-next chip text |
| date | `fields/types/date/*.mjs` | `admin/client-next/src/fields/date` | Present | Present | Present | Complete | `e2e-ui/tests/parity/item.spec.ts` covers legacy-to-next and next-to-legacy date edit persistence using `YYYY-MM-DD` with Mongo verification; `e2e-ui/tests/parity/list.spec.ts` covers URL-backed `publishedAt` date filter parity plus interactive after-filter parity in both clients |
| datearray | `fields/types/datearray/*.mjs` | `admin/client-next/src/fields/datearray` | Present | Present | Present | Complete | `e2e-ui/tests/parity/item.spec.ts` covers legacy per-item DateInput to admin-next comma-separated input and next-to-legacy date array edit persistence with Mongo verification; `e2e-ui/tests/parity/list.spec.ts` covers datearray presence/mode filtering with matching row IDs, Mongo verification, and admin-next chip text; `DateArrayType.updateItem` parses string inputs with the configured parse format in UTC and `DateArrayField` formats stored dates in UTC, avoiding local-timezone date shifting in persistence and legacy display |
| datetime | `fields/types/datetime/*.mjs` | `admin/client-next/src/fields/datetime` | Present | Present | Present | Complete | `e2e-ui/tests/parity/item.spec.ts` covers legacy-to-next and next-to-legacy split date/time edit persistence with local input-format Mongo verification, including DST-boundary dates that preserve local wall-clock values across both clients; shared list route state serializes structured datetime filter modes through the same date filter contract |
| email | `fields/types/email/*.mjs` | `admin/client-next/src/fields/email` | Present | Present | Present | Complete | `e2e-ui/tests/parity/item.spec.ts` covers legacy-to-next and next-to-legacy email edit persistence on seeded User items with Mongo verification; `e2e-ui/tests/parity/list.spec.ts` covers interactive email mode filtering in both clients with matching row IDs, Mongo verification, and admin-next chip text |
| file | `fields/types/file/*.mjs` | `admin/client-next/src/fields/file` | Present | Present | Present | Complete | `e2e-ui/tests/fields/media-upload.spec.ts` covers legacy file upload filename/public URL resolution and admin-next file upload, replacement, and remove persistence against field-complete local storage |
| geopoint | `fields/types/geopoint/*.mjs` | `admin/client-next/src/fields/geopoint` | Present | Present | Present | Complete | `e2e-ui/tests/parity/item.spec.ts` covers legacy `[lng, lat]` inputs to admin-next `{ lat, lng }` inputs and next-to-legacy geopoint edit persistence with Mongo verification; `e2e-ui/tests/parity/list.spec.ts` covers interactive geopoint proximity filtering in both clients with matching row IDs, Mongo distance verification, and admin-next chip text |
| html | `fields/types/html/*.mjs` | `admin/client-next/src/fields/html` | Present | Present | Present | Complete | `e2e-ui/tests/parity/item.spec.ts` covers legacy TinyMCE/plain textarea-to-admin-next TipTap reload and admin-next TipTap-to-legacy reload with Mongo verification; `e2e-ui/tests/parity/list.spec.ts` covers interactive HTML mode filtering in both clients with matching row IDs, Mongo verification, and admin-next chip text |
| key | `fields/types/key/*.mjs` | `admin/client-next/src/fields/key` | Present | Present | Present | Complete | `e2e-ui/tests/parity/item.spec.ts` covers legacy-to-next and next-to-legacy key edit persistence on the parity Post fixture with Mongo verification; `e2e-ui/tests/parity/list.spec.ts` covers interactive key mode filtering in both clients with matching row IDs, Mongo verification, and admin-next chip text |
| localfile | `fields/types/localfile/*.mjs` | `admin/client-next/src/fields/localfile` | Present | Present | Present | Out of scope | Removed field type: field-complete manifest records that runtime exports remain but `Types.LocalFile` is not constructible; canonical upload coverage is through `Types.File` with storage |
| localfiles | `fields/types/localfiles/*.mjs` | `admin/client-next/src/fields/localfiles` | Present | Present | Present | Out of scope | Removed field type: field-complete manifest records that runtime exports remain but `Types.LocalFiles` is not constructible; canonical upload coverage is through `Types.File` with storage |
| location | `fields/types/location/*.mjs` | `admin/client-next/src/fields/location` | Present | Present | Present | Complete | `e2e-ui/tests/parity/item.spec.ts` covers legacy-to-next and next-to-legacy address and geo subfield edit persistence with Mongo verification, plus matching Google improve/overwrite control visibility and toggling in both clients when `enableImprove` is enabled; `e2e-ui/tests/parity/list.spec.ts` covers interactive structured postcode filtering in both clients with matching row IDs, Mongo verification, and admin-next chip text |
| markdown | `fields/types/markdown/*.mjs` | `admin/client-next/src/fields/markdown` | Present | Present | Present | Complete | `e2e-ui/tests/parity/item.spec.ts` covers legacy-to-next and next-to-legacy markdown edit persistence with Mongo verification, admin-next preview rendering, and script sanitization evidence; admin-next preview now refreshes when an item value loads after lazy preview libraries; `e2e-ui/tests/parity/list.spec.ts` covers interactive Markdown mode filtering in both clients with matching row IDs, Mongo verification, and admin-next chip text |
| money | `fields/types/money/*.mjs` | `admin/client-next/src/fields/money` | Present | Present | Present | Complete | `e2e-ui/tests/parity/item.spec.ts` covers legacy-to-next and next-to-legacy decimal money edit persistence on the parity Post fixture with Mongo verification, including legacy currency-symbol input normalization; `e2e-ui/tests/parity/list.spec.ts` covers interactive less-than money filtering in both clients with matching row IDs, Mongo verification, and admin-next chip text |
| name | `fields/types/name/*.mjs` | `admin/client-next/src/fields/name` | Present | Present | Present | Complete | `e2e-ui/tests/parity/item.spec.ts` covers legacy-to-next and next-to-legacy first/last name edit persistence on seeded User items with Mongo verification; `e2e-ui/tests/parity/list.spec.ts` covers interactive name mode filtering in both clients with matching row IDs, Mongo verification, and admin-next chip text |
| number | `fields/types/number/*.mjs` | `admin/client-next/src/fields/number` | Present | Present | Present | Complete | `e2e-ui/tests/parity/item.spec.ts` covers legacy-to-next and next-to-legacy number edit persistence with Mongo verification |
| numberarray | `fields/types/numberarray/*.mjs` | `admin/client-next/src/fields/numberarray` | Present | Present | Present | Complete | `e2e-ui/tests/parity/item.spec.ts` covers legacy per-item input to admin-next comma-separated input and next-to-legacy number array edit persistence with Mongo verification; `e2e-ui/tests/parity/list.spec.ts` covers interactive numberarray presence/mode filtering in both clients with matching row IDs, Mongo verification, and admin-next chip text |
| password | `fields/types/password/*.mjs` | `admin/client-next/src/fields/password` | Present | Present | Present | Complete | `e2e-ui/tests/parity/item.spec.ts` covers admin-next password confirmation mismatch validation, server 400 handling, field error rendering, and stored hash preservation; `e2e-ui/tests/parity/fields.spec.ts` covers admin-next password change hashing and hash non-rendering; `e2e-ui/tests/parity/list.spec.ts` covers interactive password existence filtering in both clients |
| relationship | `fields/types/relationship/*.mjs` | `admin/client-next/src/fields/relationship` | Present | Present | Present | Complete | `e2e-ui/tests/parity/relationships.spec.ts` and `e2e-ui/tests/parity/fields.spec.ts` cover display/search exclusion, optional empty single/many render/save/reload parity, direct legacy-vs-next save/remove reloads, relationship create-inline creation/selection/persistence in both clients, admin-next single/many save/remove/reload persistence, and required single/many relationship validation with stored-value preservation |
| select | `fields/types/select/*.mjs` | `admin/client-next/src/fields/select` | Present | Present | Present | Complete | `e2e-ui/tests/parity/item.spec.ts` covers legacy-to-next and next-to-legacy text and numeric select edit persistence with Mongo verification; `e2e-ui/tests/parity/fields.spec.ts` covers admin-next empty-option round trip |
| text | `fields/types/text/*.mjs` | `admin/client-next/src/fields/text` | Present | Present | Present | Complete | `e2e-ui/tests/parity/item.spec.ts` covers required title edits and optional content edits in both clients, with cross-UI reload and Mongo verification; `e2e-ui/tests/parity/list.spec.ts` covers URL-backed and interactive text mode filters in both clients with matching row IDs, Mongo verification, and admin-next chip text |
| textarea | `fields/types/textarea/*.mjs` | `admin/client-next/src/fields/textarea` | Present | Present | Present | Complete | `e2e-ui/tests/parity/item.spec.ts` covers legacy-to-next and next-to-legacy multi-line textarea edit persistence with Mongo verification; `e2e-ui/tests/parity/list.spec.ts` covers interactive textarea mode filtering in both clients with matching row IDs, Mongo verification, and admin-next chip text |
| textarray | `fields/types/textarray/*.mjs` | `admin/client-next/src/fields/textarray` | Present | Present | Present | Complete | `e2e-ui/tests/parity/item.spec.ts` covers legacy per-item input to admin-next comma-separated input and next-to-legacy text array edit persistence with Mongo verification; `e2e-ui/tests/parity/list.spec.ts` covers interactive textarray presence/mode filtering in both clients with matching row IDs, Mongo verification, and admin-next chip text |
| url | `fields/types/url/*.mjs` | `admin/client-next/src/fields/url` | Present | Present | Present | Complete | `e2e-ui/tests/parity/item.spec.ts` covers legacy-to-next and next-to-legacy URL edit persistence with Mongo verification; `e2e-ui/tests/parity/list.spec.ts` covers interactive URL mode filtering in both clients with matching row IDs, Mongo verification, and admin-next chip text |
